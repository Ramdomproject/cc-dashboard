/** @type {import('next').NextConfig} */
const nextConfig = {
  // Allow inline event handlers in dangerouslySetInnerHTML
  // (dashboard uses onclick/onchange attributes from legacy HTML)
};

export default nextConfig;